using UnityEngine;

/// <summary>
/// Kept temporarily so existing scene references remain valid after replacing the old
/// Map -> Track -> Mission flow. The new MapSelect carousel no longer uses this component.
/// </summary>
public class TrackPanel : MonoBehaviour
{
}
